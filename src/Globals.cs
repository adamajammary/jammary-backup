namespace JammaryBackup
{
    /// <summary>
    /// Enumeration containing a Success or Failed status.
    /// <para>Used as a return variable for methods to determine if they failed or succeeded.</para>
    /// </summary>
    public enum Result
    {
        Success, Failed
    }
}
